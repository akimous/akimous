{
    "name": "chakuro",
    "version": "1.0.0",
    "description": "A progressive Python IDE",
    "main": "build/index.html",
    "repository": "git@gitlab.com:red8012/chakuro.git",
    "author": "Yu-Ann Chen <red8012@gmail.com>",
    "license": "MIT",
    "private": true,
    "devDependencies": {
        "babel-cli": "^6.26.0",
        "babel-core": "6",
        "babel-minify": "^0.3.0",
        "browser-sync": "^2.23.1",
        "codemirror": "^5.33.0",
        "eslint": "^4.18.2",
        "http-server": "^0.11.1",
        "keyboardjs": "^2.4.1",
        "normalize.css": "^8.0.0",
        "pressure": "^2.1.2",
        "rollup": "^0.57.0",
        "rollup-plugin-babel": "^3.0.3",
        "rollup-plugin-commonjs": "^9.1.0",
        "rollup-plugin-css-only": "^0.4.0",
        "rollup-plugin-node-resolve": "^3.2.0",
        "rollup-plugin-postcss": "^1.3.4",
        "rollup-plugin-sizes": "^0.4.2",
        "rollup-plugin-strip": "^1.1.1",
        "rollup-plugin-svelte": "^4.0.0",
        "sortablejs": "^1.7.0",
        "svelte": "^1.57.4",
        "svelte-cli": "^2.0.0",
        "xterm": "^3.2.0"
    },
    "dependencies": {},
    "scripts": {
        "dev": "browser-sync start --server 'dist' --files 'dist' & rollup -c -w",
        "build": "mkdir -p dist && rm -rf dist/* && cp -r src/index.html resources/* dist/ && rollup -c",
        "copystatic": "cp -r src/index.html resources/* dist/",
        "release": "mkdir -p dist && rm -rf dist/* && cp -r src/index.html resources/* dist/ && rollup -c && cd dist/ && zopfli bundle.* && brotli bundle.js",
        "start": "http-server ./dist -p 5000 -g"
    }
}
